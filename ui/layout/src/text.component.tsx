import styled                            from '@emotion/styled'
import { space, flexbox, layout, color, typography  } from 'styled-system'

export const Text = styled('span')(space, flexbox, layout, color,typography )
